<% title "Api/Exo Images" %>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% for exo_image in @exo_images %>
    <tr>
      <td><%= exo_image.name %></td>
      <td><%= link_to "Show", [:api, exo_image] %></td>
      <td><%= link_to "Edit", edit_api_exo_image_path(exo_image) %></td>
      <td><%= link_to "Destroy", [:api, exo_image], :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Api/Exo Image", new_api_exo_image_path %></p>
