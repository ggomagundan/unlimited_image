<%= form_for(@unlimited) do |f| %>
  <% if @unlimited.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unlimited.errors.count, "error") %> prohibited this unlimited from being saved:</h2>

      <ul>
      <% @unlimited.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
